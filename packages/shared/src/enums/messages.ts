export enum IRecipientType {
  ACCOUNT = "single",
  CLASS = "class",
  GROUP = "group"
}

export enum IThreadFolders {
  INBOX = "inbox",
  OUTBOX = "outbox",
  ARCHIVE = "archive",
  DRAFTS = "drafts",
  ALL = "all"
}